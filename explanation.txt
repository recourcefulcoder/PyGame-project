Переделал detect – раньше был перегруженный, теперь разделён на detect_mine (светин код) и detect_tower 
(который обозначает клетки поля, поражаемые бомбой)

Карта уровня вынесена в поле класса player – иначе нельзя корректировать уровень (взрыв вышек)

Удалил Player из generate_level (вспомогательный класс в этом файле, который давно следовало выкинуть)

В попытках создать возможность менять спрайт вышки на спрайт руин создал систему – спрайты вышек будут
при отрисовке уровня выносится в отдельный словарь, который будет сохранятся в полях класса player - в связи с этим 
функция generate_level вынесена из основного класса программы в инициализатор игрока.

Ключи словаря towers_dict в generate_level – это кортеж координат

Почему-то функция exploded_cells работает раз через три – а в остальном реализовал задачу)
